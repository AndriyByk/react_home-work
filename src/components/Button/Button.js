import React from 'react';

import '../../App.css';

const Button = () => {
    return (
        <div className={'button'}>
            <button>
                Find
            </button>
        </div>
    );
};

export default Button;