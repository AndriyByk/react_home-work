import React from 'react';

const FilterButton = () => {


    return (
        <div className={'button'}>
            {/*<button onClick={func()}>*/}
            <button>
                Filter
            </button>
        </div>
    );
};

export default FilterButton;